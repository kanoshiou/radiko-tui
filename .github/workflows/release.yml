name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build
    strategy:
      matrix:
        include:
          - os: windows-latest
            output: radiko-windows-amd64.exe
          - os: ubuntu-latest
            output: radiko-linux-amd64
    
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev

      - name: Build
        run: go build -ldflags="-s -w" -o ${{ matrix.output }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}
          path: ${{ matrix.output }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Move artifacts to root
        run: |
          find artifacts -type f -exec mv {} . \;

      - name: Create checksums
        run: |
          sha256sum radiko-* > checksums.txt

      - name: Get version
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            radiko-windows-amd64.exe
            radiko-linux-amd64
            checksums.txt
          body: |
            ## Radiko JP Player ${{ steps.get_version.outputs.VERSION }}
            
            ### 📦 下载
            
            选择适合你系统的版本：
            
            - **Windows (x64)**: `radiko-windows-amd64.exe`
            - **Linux (x64)**: `radiko-linux-amd64`
            
            ### ⚠️ 重要提示
            
            **需要安装 ffmpeg**：本程序依赖 ffmpeg 进行音频解码。
            
            - Windows: `choco install ffmpeg`
            - Linux: `sudo apt install ffmpeg`
            - macOS: `brew install ffmpeg`
            
            ### 🎵 音量控制
            
            - 启动时设置音量：`radiko.exe --volume 80`
            - 运行时按 ↑/↓ 调节音量
            - 按 m 键静音/取消静音
            - 按 0-9 快速设置音量
            
            ### ✅ 校验和
            
            下载后请验证文件完整性，参考 `checksums.txt`
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
